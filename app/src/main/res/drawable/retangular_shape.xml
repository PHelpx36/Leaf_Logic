<!-- res/drawable/rounded_rectangle.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Sombra -->
    <item android:right="4dp" android:left="4dp" android:top="4dp" android:bottom="4dp">
        <shape android:shape="rectangle">
            <corners android:radius="16dp" />
            <solid android:color="#80000000" /> <!-- Sombra preta com transparência -->
        </shape>
    </item>

    <!-- Retângulo com cantos arredondados -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="16dp" /> <!-- Define o raio das bordas -->
            <stroke android:width="2dp" android:color="#FFFFFF" /> <!-- Borda branca -->
            <solid android:color="#4EED55" /> <!-- Cor verde -->
        </shape>
    </item>
</layer-list>
