<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fundo personalizado do Spinner -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/opacity_black" />
            <padding android:left="20dp" android:right="10dp" />
        </shape>
    </item>

    <!-- Ícone de seta à direita -->
    <item android:drawable="@drawable/baseline_arrow_drop_down_24" android:gravity="end|center_vertical" android:right="10dp"/>
</layer-list>